name: Deploy to Nekoweb

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
        - name: Checkout code
          uses: actions/checkout@v4

        - name: Deploy to NekoWeb
          uses: indiefellas/deploy2nekoweb@main
          with:
            nekoweb-api-key: ${{ secrets.NEKOWEB_API_KEY }}
            nekoweb-domain: 'https://venus-territory.nekoweb.org/'
            nekoweb-username: 'venus-territory'
            directory: 'venus-territory.nekoweb.org'